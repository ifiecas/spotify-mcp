swagger: '2.0'
info:
  title: 🎧 Spotify MCP Server
  description: >
    A Model Context Protocol (MCP) server exposing Spotify Web API data.
    Tools include artist search, top tracks, albums, audio features, and artist profile summary.
  version: 1.0.0
host: spotify-mcp1011-fzepetdfcqhsa8fg.australiaeast-01.azurewebsites.net
basePath: /
schemes:
  - https
paths:
  /mcp:
    post:
      summary: Invoke Spotify MCP Tools
      description: Streamable MCP endpoint compatible with Copilot Studio.
      operationId: InvokeSpotifyMCP
      x-ms-agentic-protocol: mcp-streamable-1.0
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - name: tool
          in: body
          required: true
          description: Name of the MCP tool to invoke
          schema:
            type: string
        - name: args
          in: body
          required: false
          description: Arguments for the selected tool
          schema:
            type: object
      responses:
        '200':
          description: Success — tool result returned
        '400':
          description: Bad request — invalid tool name or arguments
        '500':
          description: Internal error
